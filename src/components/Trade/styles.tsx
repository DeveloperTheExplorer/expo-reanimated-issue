import { StyleSheet } from 'react-native';
import { Colors } from 'react-native-ui-lib';

export default StyleSheet.create({
    container: {
        borderBottomColor: Colors.$backgroundPrimaryLight,
        borderBottomWidth: 1
    },
    nftImage: {
        width: 60,
        height: 60,
        borderRadius: 5
    }
})